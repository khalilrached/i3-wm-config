# My config files for i3 wm on ubuntu 

clone this repository and place it under `~/.config` file after install all the dependencies

### dependencies 

`i3` `rofi` `i3blocks` `compton` `thunar` `lxappearance` `Font Awesome` 
